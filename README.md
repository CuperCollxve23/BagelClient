# BagelClient

## Run locally

This project expects `index.html` to be served over HTTP.

Run the local server from the repository root:

```bash
./run-local-server.sh
```

Then open:

```text
http://127.0.0.1:8000
```

Click `Launch 1.12.2` and, if needed, click inside the game window to enable keyboard input.
